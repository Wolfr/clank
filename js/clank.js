Zepto(function($){

  // Fingerblast!
  var fb = new FingerBlast('.cl-toggle, .cl-slider');

  // Add data ignore push to links unrelated to demo
  // $('#header a, .cl-demo-list a').attr('data-ignore', 'push');

})
